name: My first workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello, GitHub Actions!"
      - name: Print name 
        run: echo "Prabhgun"

      
  say-name:
    runs-on: ubuntu-latest 

    steps:
      - name: Print name
        run: echo "Prabhgun"
